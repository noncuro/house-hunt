{
  "//": "For the repo-root tools/ scripts, which run under tsx and reach into the packages.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "allowImportingTsExtensions": true
  },
  "include": ["tools"],
  "exclude": ["supabase/functions"]
}
